/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  basePath: "/christianesguerra",
  images: { unoptimized: true },
  trailingSlash: true,
};

module.exports = nextConfig;
